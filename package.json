{
  "name": "dsh-sidebar",
  "version": "0.1.0",
  "description": "Right-side file workspace for DeepSeek Harness: top-bar button opens a resizable right panel with a file tree (content manager) and a text editor, scoped to the current session workspace.",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./client": "./lib/client.js",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "cordis.patch.yml"
  ],
  "scripts": {
    "build": "node scripts/build-client.mjs",
    "test": "node scripts/test-server.mjs && node scripts/test-client-render.mjs && node scripts/test-client-theme.mjs && node scripts/test-terminal.mjs"
  },
  "license": "MIT",
  "dsh": {
    "client": {
      "platform": "web",
      "inject": [
        "@deepseek-ai/dsh-client-runtime",
        "@deepseek-ai/dsh-client-ui-slots"
      ]
    },
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  }
}